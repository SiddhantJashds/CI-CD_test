on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    environment: DEV
    steps:
      - name: print environment
        run: echo "this is running on ${{secrets.ENV_TYPE}}"
      - run: python3 --version
      - run: echo "this is running on ${{vars.ENV_TYPE_V2}}"
